using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DoorInteraction : UITextTypingEffect
{
    protected override void Update() {
        base.Update();

        if(GameManager.isUserHaveKey == true)
        {
            
        }
    }
}
